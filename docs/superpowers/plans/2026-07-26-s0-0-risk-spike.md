# S0-0 风险验证 Spike Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用约 2 人日证伪 S0 剩余的三个未知，任一项失败都会作废 13–19 人日中的大部分。

**Architecture:** 三条互相独立的验证线，每条产出一份可据以决策的结论文档。U2（Seatbelt）纯本机、可全自动；U1（OAuth）与 U3（Tunnel）需要真人在 ChatGPT / OpenAI Platform 侧操作。Spike 代码一次性，结论进规格。

**Tech Stack:** Node 24 · TypeScript · pnpm · `sandbox-exec`(SBPL) · `@modelcontextprotocol/sdk@1.29.0` · Hono · `cloudflared`(现有) · `tunnel-client`(U3 新增)

## 三个未知

| # | 未知 | 若失败 | 规格出处 |
|---|---|---|---|
| **U1** | ChatGPT 能否与要求 OAuth 2.1 + PKCE 的 MCP 服务端完成握手 | **阻塞 S0** —— POC 用的是 No Authentication，这条从未验证 | 观察记录「未覆盖项 1」 |
| **U2** | `(deny default)` + `(deny network*)` 下，node/npm/vitest 能否真正跑起来 | S0-C 的 Seatbelt 方案不成立，须回退到容器（推翻 D1） | 规格 §12.1 第 4、5 条 |
| **U3** | Secure MCP Tunnel 的长轮询给调用往返加多少延迟 | 若显著拖慢，D13 需重新评估；且 P-1 的 `HUMAN_GAP_MS` 阈值论证需重算 | 规格 D13、调研 §5.1 |

## Global Constraints

以下取自规格 `docs/superpowers/specs/2026-07-25-grande-gpt-s0-design.md`，**每个任务的要求都隐含包含本节**。

- **Spike 代码一次性，全部放在 `spike/`，不进入 S0 代码库。** 交付物是三份结论 + 对规格的修订。
- **`sandbox-exec` 的 SBPL 按「最具体规则优先」裁决，不是按书写顺序**（2026-07-25 已实测：`deny 父目录` + `allow 子目录` 下子目录可读）。
- **沙箱内可写根只有当前任务 worktree**；`<canonical>/.git` 与 `<worktree>/.git` 均不可写（git hooks 为所有 worktree 共享）；控制平面根不可读。
- **环境变量必须清洗**：子进程只传 `PATH` / `HOME=<jobTmp>/home` / `LANG` / `TMPDIR=<jobTmp>`，剥离所有 `DYLD_*`。
- **必须杀整个进程组**（`kill(-pgid)`），不能只杀 `sandbox-exec`。
- **合规红线**：U1/U3 全程真人手动操作 ChatGPT，禁止脚本化或无人值守驱动。
- Node 24 原生剥离 TypeScript 类型，脚本不加 `--experimental-strip-types`。
- `@modelcontextprotocol/sdk` 锁定 `1.29.0`；transport 必须用
  `WebStandardStreamableHTTPServerTransport`（`server/webStandardStreamableHttp.js`），
  **不是**同名的 Node 风格 `StreamableHTTPServerTransport`。

## 已知的现场事实（不必重新发现）

- `poc/node_modules` 由 pnpm 安装，`node_modules/<pkg>` 是**相对**符号链接指向 `node_modules/.pnpm/<pkg>@<ver>/node_modules/<pkg>`，**全部留在 `node_modules` 内**。
- pnpm 全局 store 在 `/Users/xtation/Library/pnpm/store/v10`（**在 GPT_Workspace 之外**）。pnpm 默认把文件**硬链接**进 store——硬链接对文件系统透明（同 inode、无路径间接），因此**理论上不需要给 store 放行**。**这正是 U2 要验证的一条。**
- 文件系统是 APFS，`cp -Rc`（clonefile）可用且保留符号链接。
- `(deny network*)` 已实测：DNS 与直连 IP 均在 socket 层被拒。

## File Structure

```
spike/
├── package.json
├── tsconfig.json
├── src/
│   ├── sbpl.ts              # SBPL profile 生成（纯函数，S0-C 会重写但结论复用）
│   └── sandbox.ts           # sandbox-exec 执行器：清洗环境、进程组、超时、资源轮询
├── oauth/                   # U1：最小 OAuth AS + 受保护的 MCP 服务端
│   ├── server.ts
│   └── README.md
├── tests/
│   ├── sbpl.test.ts
│   └── sandbox.test.ts
└── findings/                # 三份结论（本 spike 的真正交付物）
    ├── U1-oauth.md
    ├── U2-seatbelt.md
    └── U3-tunnel.md
```

**职责边界**：`sbpl.ts` 只生成策略文本（纯函数、易测）；`sandbox.ts` 只管进程生命周期与资源。二者分开的理由是前者可以穷举测试，后者必须跑真进程。

---

## Task 1: SBPL 生成器与沙箱执行器

**Files:**
- Create: `spike/package.json`, `spike/tsconfig.json`, `spike/.gitignore`
- Create: `spike/src/sbpl.ts`
- Create: `spike/src/sandbox.ts`
- Test: `spike/tests/sbpl.test.ts`, `spike/tests/sandbox.test.ts`

**Interfaces:**
- Consumes: 无
- Produces:
  - `interface SandboxPaths { worktree: string; canonicalGit: string; jobTmp: string; controlRoot: string; worktreesRoot: string }`
  - `function buildProfile(p: SandboxPaths): string`
  - `interface RunOptions { argv: string[]; cwd: string; paths: SandboxPaths; timeoutMs: number; maxOutputBytes: number; maxRssMb?: number }`
  - `interface RunResult { exitCode: number | null; stdout: string; stderr: string; truncated: boolean; killedBy: null | "timeout" | "rss" | "output"; durationMs: number; peakRssMb: number }`
  - `function runSandboxed(o: RunOptions): Promise<RunResult>`

- [ ] **Step 1: 建脚手架**

```bash
mkdir -p spike/src spike/tests spike/findings spike/oauth
```

`spike/package.json`：

```json
{
  "name": "grande-gpt-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "@hono/node-server": "1.19.7",
    "hono": "4.12.32",
    "jose": "6.1.0",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
```

`spike/tsconfig.json`：

```json
{
  "compilerOptions": {
    "target": "ES2023",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2023"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noEmit": true,
    "allowImportingTsExtensions": true,
    "skipLibCheck": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "tests/**/*.ts", "oauth/**/*.ts"]
}
```

`spike/.gitignore`：

```
node_modules/
.env
*.log
sandbox-*.sb
```

Run: `cd spike && pnpm install`
Expected: 安装成功。

- [ ] **Step 2: 写 sbpl 的失败测试**

`spike/tests/sbpl.test.ts`：

```typescript
import { describe, expect, it } from "vitest";
import { buildProfile, type SandboxPaths } from "../src/sbpl.ts";

const paths: SandboxPaths = {
  worktree: "/W/.grande-work/worktrees/demo/task_1",
  canonicalGit: "/W/demo/.git",
  jobTmp: "/tmp/job_1",
  controlRoot: "/Users/u/.grande-control",
  worktreesRoot: "/W/.grande-work/worktrees",
};

describe("buildProfile()", () => {
  it("以 deny default 开头并全局禁网", () => {
    const p = buildProfile(paths);
    expect(p).toContain("(deny default)");
    expect(p).toContain("(deny network*)");
  });

  it("worktree 可写", () => {
    expect(buildProfile(paths)).toContain(`(allow file-write* (subpath "${paths.worktree}"))`);
  });

  it("canonical 的 .git 不可写（hooks 为所有 worktree 共享）", () => {
    expect(buildProfile(paths)).toContain(`(deny file-write* (subpath "${paths.canonicalGit}"))`);
  });

  it("控制平面根不可读", () => {
    expect(buildProfile(paths)).toContain(`(deny file-read* (subpath "${paths.controlRoot}"))`);
  });

  it("先 deny worktrees 父目录、再 allow 本任务 worktree（依赖最具体规则优先）", () => {
    const p = buildProfile(paths);
    const denyIdx = p.indexOf(`(deny file-read* (subpath "${paths.worktreesRoot}"))`);
    const allowIdx = p.indexOf(`(allow file-read* (subpath "${paths.worktree}"))`);
    expect(denyIdx).toBeGreaterThan(-1);
    expect(allowIdx).toBeGreaterThan(-1);
  });

  it("路径中的双引号被转义，不能截断 SBPL 字符串", () => {
    const evil = { ...paths, worktree: '/W/a"b' };
    const p = buildProfile(evil);
    expect(p).toContain('/W/a\\"b');
    expect(p).not.toContain('"/W/a"b"');
  });

  it("拒绝相对路径——SBPL 的 subpath 必须是绝对路径", () => {
    expect(() => buildProfile({ ...paths, worktree: "relative/path" })).toThrow(/绝对路径/);
  });
});
```

- [ ] **Step 3: 运行确认失败**

Run: `cd spike && pnpm test tests/sbpl.test.ts`
Expected: FAIL —— 无法解析 `../src/sbpl.ts`

- [ ] **Step 4: 实现 sbpl.ts**

`spike/src/sbpl.ts`：

```typescript
import { isAbsolute } from "node:path";

export interface SandboxPaths {
  /** 当前任务 worktree —— 唯一可写的仓库路径 */
  worktree: string;
  /** canonical 仓库的 .git 目录。worktree 里的 .git 是个文件、指向这里，
   *  而 hooks 存放于此且为所有 worktree 共享——必须整体不可写 */
  canonicalGit: string;
  /** 本 job 的临时目录，同时用作 HOME 与 TMPDIR */
  jobTmp: string;
  /** 控制平面根（状态/配置/审计）——被审计者不能读 */
  controlRoot: string;
  /** 全部 worktree 的父目录——先整体拒读，再单独放行本任务的 */
  worktreesRoot: string;
}

/** SBPL 字符串字面量里只需转义反斜杠与双引号 */
function q(path: string): string {
  if (!isAbsolute(path)) throw new Error(`SBPL 的 subpath 必须是绝对路径，收到：${path}`);
  return path.replace(/\\/g, "\\\\").replace(/"/g, '\\"');
}

/**
 * 生成本 job 的 SBPL 策略。
 *
 * 裁决语义（2026-07-25 于 macOS 26.5.1 实测）：**按最具体规则优先，不是按书写顺序**。
 * 因此 `deny (subpath worktreesRoot)` + `allow (subpath worktree)` 能实现
 * 「只见自己、不见他人」，且新建的 worktree 自动被父目录的 deny 覆盖，
 * 无需在每次 job 启动时枚举其他任务。
 *
 * 读权限整体放宽（除控制平面根与他人 worktree 外），因为 node/npm/tsc 会读大量
 * 意想不到的系统路径，逐目录白名单会陷入无穷调试；而全禁网意味着读到的东西出不去。
 */
export function buildProfile(p: SandboxPaths): string {
  return [
    "(version 1)",
    "(deny default)",
    "(deny network*)",
    "",
    ";; 读：整体放宽，再挖掉两块",
    "(allow file-read*)",
    `(deny file-read* (subpath "${q(p.controlRoot)}"))`,
    `(deny file-read* (subpath "${q(p.worktreesRoot)}"))`,
    `(allow file-read* (subpath "${q(p.worktree)}"))`,
    "",
    ";; 写：只有本任务 worktree 与本 job 临时目录",
    `(allow file-write* (subpath "${q(p.worktree)}"))`,
    `(allow file-write* (subpath "${q(p.jobTmp)}"))`,
    `(deny file-write* (subpath "${q(p.canonicalGit)}"))`,
    `(deny file-write* (subpath "${q(p.worktree)}/.git"))`,
    "",
    ";; 执行",
    '(allow process-exec (subpath "/usr/bin") (subpath "/bin") (subpath "/usr/sbin") (subpath "/opt/homebrew"))',
    "(allow process-fork)",
    "(allow sysctl-read)",
    "",
  ].join("\n");
}
```

- [ ] **Step 5: 运行确认通过**

Run: `cd spike && pnpm test tests/sbpl.test.ts && pnpm typecheck`
Expected: 7 个用例全部 PASS，typecheck 无错误。

- [ ] **Step 6: 写 sandbox 执行器的失败测试**

`spike/tests/sandbox.test.ts`：

```typescript
import { mkdirSync, mkdtempSync, rmSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { afterEach, beforeEach, describe, expect, it } from "vitest";
import type { SandboxPaths } from "../src/sbpl.ts";
import { runSandboxed } from "../src/sandbox.ts";

let root: string;
let paths: SandboxPaths;

beforeEach(() => {
  root = mkdtempSync(join(tmpdir(), "spike-"));
  paths = {
    worktree: join(root, "worktree"),
    canonicalGit: join(root, "canonical", ".git"),
    jobTmp: join(root, "jobtmp"),
    controlRoot: join(root, "control"),
    worktreesRoot: join(root, "worktrees"),
  };
  for (const d of [paths.worktree, paths.canonicalGit, paths.jobTmp, paths.controlRoot, paths.worktreesRoot]) {
    mkdirSync(d, { recursive: true });
  }
});

afterEach(() => rmSync(root, { recursive: true, force: true }));

describe("runSandboxed()", () => {
  it("可写根内写入成功", async () => {
    const r = await runSandboxed({
      argv: ["/bin/sh", "-c", `echo hi > ${paths.worktree}/a.txt && echo done`],
      cwd: paths.worktree,
      paths,
      timeoutMs: 10_000,
      maxOutputBytes: 65_536,
    });
    expect(r.exitCode).toBe(0);
    expect(r.stdout).toContain("done");
  });

  it("控制平面根不可读", async () => {
    writeFileSync(join(paths.controlRoot, "secret.txt"), "TOPSECRET");
    const r = await runSandboxed({
      argv: ["/bin/cat", join(paths.controlRoot, "secret.txt")],
      cwd: paths.worktree,
      paths,
      timeoutMs: 10_000,
      maxOutputBytes: 65_536,
    });
    expect(r.exitCode).not.toBe(0);
    expect(r.stdout).not.toContain("TOPSECRET");
  });

  it("canonical 的 .git 不可写", async () => {
    const r = await runSandboxed({
      argv: ["/bin/sh", "-c", `echo x > ${paths.canonicalGit}/hooks-probe`],
      cwd: paths.worktree,
      paths,
      timeoutMs: 10_000,
      maxOutputBytes: 65_536,
    });
    expect(r.exitCode).not.toBe(0);
  });

  it("网络被拒", async () => {
    const r = await runSandboxed({
      argv: ["/usr/bin/curl", "-sS", "--max-time", "5", "http://1.1.1.1"],
      cwd: paths.worktree,
      paths,
      timeoutMs: 15_000,
      maxOutputBytes: 65_536,
    });
    expect(r.exitCode).not.toBe(0);
  });

  it("环境变量被清洗：宿主的密钥不进沙箱", async () => {
    process.env.SPIKE_FAKE_TOKEN = "should-not-leak";
    try {
      const r = await runSandboxed({
        argv: ["/usr/bin/env"],
        cwd: paths.worktree,
        paths,
        timeoutMs: 10_000,
        maxOutputBytes: 65_536,
      });
      expect(r.stdout).not.toContain("should-not-leak");
      expect(r.stdout).toContain(`HOME=${paths.jobTmp}/home`);
    } finally {
      delete process.env.SPIKE_FAKE_TOKEN;
    }
  });

  it("超时杀掉整个进程组，孤儿不残留", async () => {
    const marker = join(root, "orphan-alive");
    const r = await runSandboxed({
      // 子进程每秒往 marker 追加一行；父进程 sleep。超时后两者都应停止。
      argv: ["/bin/sh", "-c", `( while true; do echo x >> ${marker}; sleep 1; done ) & sleep 60`],
      cwd: paths.worktree,
      paths,
      timeoutMs: 3_000,
      maxOutputBytes: 65_536,
    });
    expect(r.killedBy).toBe("timeout");
    const { readFileSync, existsSync } = await import("node:fs");
    const before = existsSync(marker) ? readFileSync(marker, "utf8").length : 0;
    await new Promise((res) => setTimeout(res, 2500));
    const after = existsSync(marker) ? readFileSync(marker, "utf8").length : 0;
    expect(after, "超时后子进程仍在写文件，说明进程组没杀干净").toBe(before);
  });

  it("输出超限时截断并标记", async () => {
    const r = await runSandboxed({
      argv: ["/bin/sh", "-c", "for i in $(seq 1 100000); do echo AAAAAAAAAAAAAAAAAAAA; done"],
      cwd: paths.worktree,
      paths,
      timeoutMs: 20_000,
      maxOutputBytes: 4096,
    });
    expect(r.truncated).toBe(true);
    expect(Buffer.byteLength(r.stdout, "utf8")).toBeLessThanOrEqual(8192);
  });
});
```

- [ ] **Step 7: 运行确认失败**

Run: `cd spike && pnpm test tests/sandbox.test.ts`
Expected: FAIL —— 无法解析 `../src/sandbox.ts`

- [ ] **Step 8: 实现 sandbox.ts**

`spike/src/sandbox.ts`：

```typescript
import { execFileSync, spawn } from "node:child_process";
import { mkdirSync, writeFileSync } from "node:fs";
import { join } from "node:path";
import { buildProfile, type SandboxPaths } from "./sbpl.ts";

export interface RunOptions {
  argv: string[];
  cwd: string;
  paths: SandboxPaths;
  timeoutMs: number;
  maxOutputBytes: number;
  /** 进程组总 RSS 上限（MB）。省略则不做内存兜底 */
  maxRssMb?: number;
}

export interface RunResult {
  exitCode: number | null;
  stdout: string;
  stderr: string;
  truncated: boolean;
  killedBy: null | "timeout" | "rss" | "output";
  durationMs: number;
  peakRssMb: number;
}

/** 每 2 秒采一次进程组总 RSS。这不是 cgroup——采样窗口内仍可冲高，是已接受的取舍 */
const RSS_POLL_MS = 2000;

function groupRssMb(pgid: number): number {
  try {
    const out = execFileSync("/bin/ps", ["-o", "rss=", "-g", String(pgid)], { encoding: "utf8" });
    const kb = out.split("\n").reduce((s, l) => s + (Number(l.trim()) || 0), 0);
    return Math.round(kb / 1024);
  } catch {
    return 0;
  }
}

export async function runSandboxed(o: RunOptions): Promise<RunResult> {
  const home = join(o.paths.jobTmp, "home");
  mkdirSync(home, { recursive: true });

  const profilePath = join(o.paths.jobTmp, "profile.sb");
  writeFileSync(profilePath, buildProfile(o.paths), "utf8");

  // 环境清洗：只传必需的四个。宿主的 *_TOKEN / *_API_KEY / DYLD_* 一律不进沙箱。
  const env = {
    PATH: "/usr/bin:/bin:/usr/sbin:/sbin:/opt/homebrew/bin",
    HOME: home,
    LANG: "en_US.UTF-8",
    TMPDIR: o.paths.jobTmp,
  };

  const started = Date.now();
  const child = spawn("/usr/bin/sandbox-exec", ["-f", profilePath, ...o.argv], {
    cwd: o.cwd,
    env,
    detached: true, // 新进程组：能一次杀掉整棵进程树
    stdio: ["ignore", "pipe", "pipe"],
  });

  const pgid = child.pid ?? 0;
  let stdout = "";
  let stderr = "";
  let bytes = 0;
  let truncated = false;
  let killedBy: RunResult["killedBy"] = null;
  let peakRssMb = 0;

  const killGroup = (reason: NonNullable<RunResult["killedBy"]>) => {
    if (killedBy) return;
    killedBy = reason;
    try {
      process.kill(-pgid, "SIGTERM");
    } catch {
      /* 已退出 */
    }
    setTimeout(() => {
      try {
        process.kill(-pgid, "SIGKILL");
      } catch {
        /* 已退出 */
      }
    }, 5000).unref();
  };

  const collect = (chunk: Buffer, into: "out" | "err") => {
    if (truncated) return;
    const remaining = o.maxOutputBytes - bytes;
    if (remaining <= 0) {
      truncated = true;
      killGroup("output");
      return;
    }
    const slice = chunk.subarray(0, remaining);
    bytes += slice.byteLength;
    if (into === "out") stdout += slice.toString("utf8");
    else stderr += slice.toString("utf8");
    if (bytes >= o.maxOutputBytes) {
      truncated = true;
      killGroup("output");
    }
  };

  child.stdout.on("data", (c: Buffer) => collect(c, "out"));
  child.stderr.on("data", (c: Buffer) => collect(c, "err"));

  const timer = setTimeout(() => killGroup("timeout"), o.timeoutMs);
  const poller = setInterval(() => {
    const mb = groupRssMb(pgid);
    if (mb > peakRssMb) peakRssMb = mb;
    if (o.maxRssMb !== undefined && mb > o.maxRssMb) killGroup("rss");
  }, RSS_POLL_MS);

  const exitCode = await new Promise<number | null>((resolve) => {
    child.on("close", (code) => resolve(code));
    child.on("error", () => resolve(null));
  });

  clearTimeout(timer);
  clearInterval(poller);

  return { exitCode, stdout, stderr, truncated, killedBy, durationMs: Date.now() - started, peakRssMb };
}
```

- [ ] **Step 9: 运行确认通过**

Run: `cd spike && pnpm test && pnpm typecheck`
Expected: sbpl 7 个 + sandbox 7 个全部 PASS。

若「超时杀掉整个进程组」用例失败，说明 `detached: true` + `kill(-pgid)` 的组合在本机没生效
—— **这是必须查清的真问题，不要放宽断言**。先用 `ps -o pid,pgid,command -g <pgid>` 手工确认
子进程的 pgid 是否与父进程一致。

- [ ] **Step 10: 提交**

```bash
cd /Users/xtation/AgentWorks/GPT_Workspace/grande-gpt
git add spike/package.json spike/pnpm-lock.yaml spike/tsconfig.json spike/.gitignore spike/src spike/tests
git commit -m "feat(spike): SBPL 生成器与沙箱执行器

为 U2 验证做准备。SBPL 按最具体规则优先裁决（已实测），因此
deny worktrees 父目录 + allow 本任务 worktree 即可实现任务间隔离，
且新建 worktree 自动被覆盖，无需每次枚举。

执行器做三件 Seatbelt 本身不做的事：清洗环境变量（宿主的 *_TOKEN /
DYLD_* 不进沙箱）、杀整个进程组（npm test 会派生子进程，只杀父进程
会留孤儿）、输出与 RSS 兜底。RSS 采样不是 cgroup，是已接受的取舍。"
```

---

## Task 2: U2 决定性验证 —— 在真实 pnpm 项目上跑 vitest

**Files:**
- Create: `spike/findings/U2-seatbelt.md`
- Modify: `spike/src/sbpl.ts`（仅当实测证明需要额外放行）

**Interfaces:**
- Consumes: `buildProfile`、`runSandboxed`（Task 1）
- Produces: `spike/findings/U2-seatbelt.md` —— S0-C 直接照抄的放行清单

**为什么拿 `poc/` 当靶子**：它是本机唯一一个用 pnpm 装好、有真实 vitest 套件的项目，
且其 `node_modules` 正是我们担心的符号链接布局。保真度最高且零搭建成本。

- [ ] **Step 1: 先在沙箱外确认基线**

```bash
cd /Users/xtation/AgentWorks/GPT_Workspace/grande-gpt/poc && pnpm test 2>&1 | tail -3
```

Expected: `Tests  135 passed (135)`。若这里就不通过，先修好再继续——否则无法区分是沙箱问题还是项目问题。

- [ ] **Step 2: 写验证脚本**

`spike/tests/u2-real-vitest.test.ts`：

```typescript
import { mkdirSync, mkdtempSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { afterEach, beforeEach, describe, expect, it } from "vitest";
import type { SandboxPaths } from "../src/sbpl.ts";
import { runSandboxed } from "../src/sandbox.ts";

const POC = "/Users/xtation/AgentWorks/GPT_Workspace/grande-gpt/poc";
let jobTmp: string;
let paths: SandboxPaths;

beforeEach(() => {
  jobTmp = mkdtempSync(join(tmpdir(), "u2-"));
  mkdirSync(join(jobTmp, "home"), { recursive: true });
  paths = {
    // 把 poc/ 当作「worktree」：它可写（vitest 会写缓存），其余边界照常
    worktree: POC,
    canonicalGit: join(POC, ".git-nonexistent"),
    jobTmp,
    controlRoot: join(tmpdir(), "u2-control-none"),
    worktreesRoot: join(tmpdir(), "u2-worktrees-none"),
  };
});

afterEach(() => rmSync(jobTmp, { recursive: true, force: true }));

describe("U2：Seatbelt 下真实 pnpm 项目能否跑通", () => {
  it("在 deny default + deny network 下，vitest 全套通过", async () => {
    const r = await runSandboxed({
      argv: ["/opt/homebrew/bin/pnpm", "test"],
      cwd: POC,
      paths,
      timeoutMs: 180_000,
      maxOutputBytes: 512_000,
    });
    // 失败时把输出打出来——这份输出就是「还需要放行什么」的清单
    if (r.exitCode !== 0) {
      console.error("=== STDOUT ===\n" + r.stdout.slice(-4000));
      console.error("=== STDERR ===\n" + r.stderr.slice(-4000));
    }
    expect(r.killedBy, "被兜底机制杀掉，不是正常退出").toBeNull();
    expect(r.exitCode, "沙箱内 pnpm test 未通过").toBe(0);
    expect(r.stdout + r.stderr).toContain("135 passed");
  }, 200_000);
});
```

- [ ] **Step 3: 运行**

Run: `cd spike && pnpm test tests/u2-real-vitest.test.ts`

**这一步的结果就是 U2 的答案。** 三种可能：

- **直接通过** → U2 PASS，pnpm 的硬链接布局确实不需要给 store 放行。记录后进 Step 4。
- **因缺少放行而失败** → 从 stderr 找出被拒的路径，在 `sbpl.ts` 里**逐条**加最小放行
  （只读优先），每加一条重跑。**每一条都要在 findings 里写明为什么需要它。**
- **被 `killedBy` 杀掉** → 调大 `timeoutMs` / `maxOutputBytes` 后重跑；若仍被杀，
  说明兜底阈值对真实测试套不合理，这本身是要记录的结论。

**不要**为了让它通过而放宽 `deny network*` 或去掉 `deny default`——那会让整个验证失去意义。

- [ ] **Step 4: 额外验证 pnpm store 与硬链接假设**

```bash
cd spike && node --input-type=module -e '
import { runSandboxed } from "./src/sandbox.ts";
import { mkdirSync, mkdtempSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
const POC="/Users/xtation/AgentWorks/GPT_Workspace/grande-gpt/poc";
const jobTmp=mkdtempSync(join(tmpdir(),"u2b-")); mkdirSync(join(jobTmp,"home"),{recursive:true});
const paths={worktree:POC,canonicalGit:join(POC,".nope"),jobTmp,
  controlRoot:"/Users/xtation/Library/pnpm/store",  // 故意把 store 设为不可读
  worktreesRoot:join(tmpdir(),"none")};
const r=await runSandboxed({argv:["/opt/homebrew/bin/pnpm","test"],cwd:POC,paths,
  timeoutMs:180000,maxOutputBytes:512000});
console.log("store 不可读时 exitCode =", r.exitCode, "killedBy =", r.killedBy);
console.log((r.stdout+r.stderr).slice(-1500));
'
```

Expected: **仍然通过**（exitCode 0）—— 证明 pnpm 的硬链接对沙箱透明，S0 不必给全局 store 放行。
若失败，则 S0-C 的 SBPL 必须加一条 store 的只读放行，写进 findings。

- [ ] **Step 5: 资源兜底实测**

```bash
cd spike && node --input-type=module -e '
import { runSandboxed } from "./src/sandbox.ts";
import { mkdirSync, mkdtempSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
const jobTmp=mkdtempSync(join(tmpdir(),"u2c-")); mkdirSync(join(jobTmp,"home"),{recursive:true});
const wt=join(jobTmp,"wt"); mkdirSync(wt,{recursive:true});
const paths={worktree:wt,canonicalGit:join(jobTmp,".nope"),jobTmp,
  controlRoot:join(jobTmp,"ctrl"),worktreesRoot:join(jobTmp,"wts")};
mkdirSync(paths.controlRoot,{recursive:true}); mkdirSync(paths.worktreesRoot,{recursive:true});

// 内存兜底：分配远超阈值
const mem=await runSandboxed({argv:["/opt/homebrew/bin/node","-e",
  "const a=[];for(;;){a.push(Buffer.alloc(50*1024*1024));}"],
  cwd:wt,paths,timeoutMs:60000,maxOutputBytes:65536,maxRssMb:300});
console.log("内存超限 → killedBy =", mem.killedBy, " peakRss =", mem.peakRssMb, "MB");

// RLIMIT_AS 是否对 Node 24 可用（规格 §6.5 要求实测再定）
const as=await runSandboxed({argv:["/bin/sh","-c",
  "ulimit -v 262144 2>/dev/null && /opt/homebrew/bin/node -e \"console.log(1)\" || echo RLIMIT_AS_UNSUPPORTED"],
  cwd:wt,paths,timeoutMs:30000,maxOutputBytes:65536});
console.log("RLIMIT_AS 探测 →", (as.stdout+as.stderr).trim().slice(0,200));
'
```

记录两项：RSS 轮询是否真的杀掉了进程（`killedBy === "rss"`）、`ulimit -v` 对 Node 24 是否可用。

- [ ] **Step 6: 写 findings/U2-seatbelt.md**

必须包含：

```markdown
# U2 · Seatbelt 可行性 —— 结论：PASS / FAIL

**日期** · **macOS 版本** · **靶子**：poc/（pnpm 安装，135 用例）

## 结论
<一句话：deny default + deny network 下 vitest 能否跑通>

## 最终放行清单（S0-C 直接照抄）
| 规则 | 为什么需要 | 不加会怎样 |
|---|---|---|
| `(allow file-read*)` + 两处 deny | ... | ... |
| ... | ... | ... |

## pnpm store
硬链接是否对沙箱透明（是否需要给 `~/Library/pnpm/store` 放行）：<结论 + 实测输出>

## 资源兜底
- RSS 轮询是否生效：<killedBy 值 + peakRss>
- `RLIMIT_AS` 对 Node 24 是否可用：<结论>。若可用则作为第一道防线，轮询降为第二道；
  若不可用，规格 §6.5「实测再定，不预先承诺」即落定为「只能靠轮询」。

## 未覆盖
<例如：Python / cargo 生态未测；npm（非 pnpm）布局未测>
```

- [ ] **Step 7: 提交**

```bash
cd /Users/xtation/AgentWorks/GPT_Workspace/grande-gpt
git add spike/findings/U2-seatbelt.md spike/src spike/tests
git commit -m "spike(U2): Seatbelt 下真实 pnpm 项目的可行性结论

拿 poc/ 当靶子——本机唯一有真实 vitest 套件且 node_modules 为 pnpm
符号链接布局的项目，保真度最高、零搭建成本。

产出 findings/U2-seatbelt.md：最终放行清单（S0-C 直接照抄）、
pnpm 硬链接是否对沙箱透明、RSS 轮询与 RLIMIT_AS 的实测结果。"
```

---

## Task 3: U1 —— OAuth 2.1 + PKCE 与 ChatGPT 的握手

**Files:**
- Create: `spike/oauth/server.ts`
- Create: `spike/oauth/README.md`
- Create: `spike/findings/U1-oauth.md`

**Interfaces:**
- Consumes: 无（独立于 Task 1、2）
- Produces: `spike/findings/U1-oauth.md`

**⚠️ 本任务需要真人在 ChatGPT 网页版操作，无法自动化完成。** 实现者做完 Step 1–3
后必须停下来，把 Step 4 的操作说明交给 Human Owner。

**规格出处**：观察记录「未覆盖项 1」写明 —— *OAuth 2.1 + PKCE 握手未验证（POC 用的是
No Authentication），S0 第一周必须单独验证，该项失败会阻塞 S0*。

- [ ] **Step 1: 实现最小 OAuth AS + 受保护的 MCP 服务端**

`spike/oauth/server.ts`：

```typescript
import { serve } from "@hono/node-server";
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { WebStandardStreamableHTTPServerTransport } from "@modelcontextprotocol/sdk/server/webStandardStreamableHttp.js";
import { Hono } from "hono";
import { cors } from "hono/cors";
import { createHash, randomBytes, randomUUID } from "node:crypto";
import { SignJWT, jwtVerify } from "jose";
import { z } from "zod";

const PORT = Number(process.env.PORT ?? 8788);
const ISSUER = process.env.ISSUER ?? `https://oauth-spike.agentjoey.ai`;
const RESOURCE = `${ISSUER}/mcp`;
const SCOPE = "grande:spike";
const KEY = new TextEncoder().encode(process.env.OAUTH_SECRET ?? randomBytes(32).toString("hex"));

/** 单用户 spike：授权码与已注册 client 都放内存 */
const codes = new Map<string, { challenge: string; clientId: string }>();
const clients = new Map<string, { redirectUris: string[] }>();

const app = new Hono();
app.use("*", cors({
  origin: "*",
  allowMethods: ["GET", "POST", "DELETE", "OPTIONS"],
  allowHeaders: ["Content-Type", "Authorization", "mcp-session-id", "Last-Event-ID", "mcp-protocol-version"],
  exposeHeaders: ["mcp-session-id", "mcp-protocol-version", "WWW-Authenticate"],
}));

// ① 受保护资源元数据 —— ChatGPT 从 401 的 WWW-Authenticate 找到这里
app.get("/.well-known/oauth-protected-resource", (c) =>
  c.json({ resource: RESOURCE, authorization_servers: [ISSUER], scopes_supported: [SCOPE] }));
app.get("/.well-known/oauth-protected-resource/mcp", (c) =>
  c.json({ resource: RESOURCE, authorization_servers: [ISSUER], scopes_supported: [SCOPE] }));

// ② 授权服务器元数据
app.get("/.well-known/oauth-authorization-server", (c) =>
  c.json({
    issuer: ISSUER,
    authorization_endpoint: `${ISSUER}/authorize`,
    token_endpoint: `${ISSUER}/token`,
    registration_endpoint: `${ISSUER}/register`,
    jwks_uri: `${ISSUER}/jwks`,
    scopes_supported: [SCOPE],
    response_types_supported: ["code"],
    grant_types_supported: ["authorization_code"],
    code_challenge_methods_supported: ["S256"],
    token_endpoint_auth_methods_supported: ["none"],
  }));

// ③ 动态客户端注册（DCR）——规范说可选，但先实现，看 ChatGPT 走不走
app.post("/register", async (c) => {
  const body = (await c.req.json()) as { redirect_uris?: string[] };
  const clientId = `client_${randomUUID()}`;
  clients.set(clientId, { redirectUris: body.redirect_uris ?? [] });
  console.log("[oauth] /register ←", JSON.stringify(body));
  return c.json({ client_id: clientId, redirect_uris: body.redirect_uris ?? [], token_endpoint_auth_method: "none" }, 201);
});

// ④ 授权端点：spike 直接同意，不做登录页
app.get("/authorize", (c) => {
  const q = c.req.query();
  console.log("[oauth] /authorize ←", JSON.stringify(q));
  const code = randomUUID();
  codes.set(code, { challenge: q.code_challenge ?? "", clientId: q.client_id ?? "" });
  const url = new URL(q.redirect_uri ?? "");
  url.searchParams.set("code", code);
  if (q.state) url.searchParams.set("state", q.state);
  return c.redirect(url.toString(), 302);
});

// ⑤ 令牌端点：校验 PKCE S256
app.post("/token", async (c) => {
  const form = await c.req.parseBody();
  console.log("[oauth] /token ←", JSON.stringify(form));
  const code = String(form.code ?? "");
  const verifier = String(form.code_verifier ?? "");
  const rec = codes.get(code);
  if (!rec) return c.json({ error: "invalid_grant" }, 400);
  codes.delete(code);

  const computed = createHash("sha256").update(verifier).digest("base64url");
  if (rec.challenge && computed !== rec.challenge) {
    console.error("[oauth] PKCE 校验失败", { computed, expected: rec.challenge });
    return c.json({ error: "invalid_grant", error_description: "PKCE mismatch" }, 400);
  }

  const token = await new SignJWT({ scope: SCOPE })
    .setProtectedHeader({ alg: "HS256" })
    .setIssuer(ISSUER)
    .setAudience(RESOURCE)
    .setSubject("spike-user")
    .setIssuedAt()
    .setExpirationTime("1h")
    .sign(KEY);
  return c.json({ access_token: token, token_type: "Bearer", expires_in: 3600, scope: SCOPE });
});

app.get("/jwks", (c) => c.json({ keys: [] })); // HS256 无公钥；仅为满足发现文档

// ⑥ 受保护的 MCP 端点
app.all("/mcp", async (c) => {
  const auth = c.req.header("authorization") ?? "";
  const bearer = auth.startsWith("Bearer ") ? auth.slice(7) : "";
  if (!bearer) {
    console.log("[oauth] /mcp ← 无 Bearer，返回 401");
    return new Response("unauthorized", {
      status: 401,
      headers: { "WWW-Authenticate": `Bearer resource_metadata="${ISSUER}/.well-known/oauth-protected-resource"` },
    });
  }
  try {
    const { payload } = await jwtVerify(bearer, KEY, { issuer: ISSUER, audience: RESOURCE });
    if (!String(payload.scope ?? "").split(" ").includes(SCOPE)) throw new Error("scope");
  } catch (e) {
    console.error("[oauth] token 校验失败", e);
    return new Response("unauthorized", {
      status: 401,
      headers: { "WWW-Authenticate": `Bearer resource_metadata="${ISSUER}/.well-known/oauth-protected-resource"` },
    });
  }

  console.log("[oauth] /mcp ← 已认证请求");
  const server = new McpServer({ name: "grande-oauth-spike", version: "0.0.0" });
  server.registerTool(
    "spike_ping",
    {
      title: "Ping",
      description: "验证 OAuth 握手成功后工具可被调用。返回一个固定字符串。",
      inputSchema: { note: z.string().optional() },
      annotations: { readOnlyHint: true, destructiveHint: false, openWorldHint: false },
    },
    async ({ note }) => ({
      content: [{ type: "text", text: `pong${note ? ` (${note})` : ""}` }],
      structuredContent: { ok: true, pong: true, note: note ?? null },
    }),
  );
  const transport = new WebStandardStreamableHTTPServerTransport();
  await server.connect(transport);
  return transport.handleRequest(c.req.raw);
});

app.get("/healthz", (c) => c.text("ok"));

serve({ fetch: app.fetch, port: PORT });
console.log(`[oauth] listening on http://127.0.0.1:${PORT}  ISSUER=${ISSUER}`);
```

- [ ] **Step 2: 本地自检发现端点与 401**

```bash
cd spike && OAUTH_SECRET=spike-dev-secret ISSUER=http://127.0.0.1:8788 PORT=8788 node oauth/server.ts &
sleep 2
curl -sS http://127.0.0.1:8788/.well-known/oauth-protected-resource | head -c 300; echo
curl -sS http://127.0.0.1:8788/.well-known/oauth-authorization-server | head -c 400; echo
curl -sS -o /dev/null -D- -X POST http://127.0.0.1:8788/mcp -H 'content-type: application/json' -d '{}' | grep -iE "HTTP/|www-authenticate"
```

Expected：两个发现文档返回 JSON；未带 Bearer 的 `/mcp` 返回 **401** 且响应头含
`WWW-Authenticate: Bearer resource_metadata="..."`。

- [ ] **Step 3: 建独立隧道并确认公网可达**

```bash
cloudflared tunnel create oauth-spike
TID=$(cloudflared tunnel list | awk '$2=="oauth-spike"{print $1}')
cat > ~/.cloudflared/oauth-spike.yml <<EOF
tunnel: $TID
credentials-file: /Users/xtation/.cloudflared/$TID.json
ingress:
  - hostname: oauth-spike.agentjoey.ai
    service: http://localhost:8788
  - service: http_status:404
EOF
cloudflared tunnel route dns oauth-spike oauth-spike.agentjoey.ai
```

**⚠️ 这一步会创建公网 DNS 记录，属对外可见操作 —— 执行前须获得 Human Owner 明确同意。**
另起终端跑隧道，并用 `ISSUER=https://oauth-spike.agentjoey.ai` 重启服务：

```bash
cloudflared tunnel --config ~/.cloudflared/oauth-spike.yml run
```

```bash
curl -sS https://oauth-spike.agentjoey.ai/healthz   # 期望 ok
```

- [ ] **Step 4: 交给 Human Owner 在 ChatGPT 中实测**

写 `spike/oauth/README.md`，内容为下述操作说明，然后**停下来等结果**：

1. 网页版 **Settings → Plugins** → **+** → 新建 developer-mode app
2. Connection 选 **Server URL**，填 `https://oauth-spike.agentjoey.ai/mcp`
3. Authentication 选 **OAuth**
4. 保存并完成授权流程
5. 新建对话，启用该 app，发送：`用这个插件 ping 一下`

**要记录的：**

- [ ] 是否出现 OAuth 授权页？流程能否走完？
- [ ] 工具列表里能否看到 `spike_ping`？
- [ ] `spike_ping` 能否成功返回 `pong`？
- [ ] 服务端日志里 `/register` 是否被调用（DCR 是否被使用）？请求体是什么？
- [ ] `/authorize` 收到的参数（尤其 `code_challenge_method` 是否为 `S256`）
- [ ] `/token` 收到的参数，PKCE 校验是否通过
- [ ] 若失败：ChatGPT 侧的报错原话 + 服务端日志的对应片段

- [ ] **Step 5: 写 findings/U1-oauth.md 并提交**

必须包含：结论（PASS/FAIL）、ChatGPT 实际走的是 DCR 还是 CIMD、发现端点的调用顺序、
PKCE 是否强制、令牌里 `aud` 的实际取值（这关系到规格 D5「每 repo 一个端点、`aud` 绑定端点」
能否落地）、以及失败时的完整错误。

```bash
cd /Users/xtation/AgentWorks/GPT_Workspace/grande-gpt
git add spike/oauth spike/findings/U1-oauth.md
git commit -m "spike(U1): OAuth 2.1 + PKCE 与 ChatGPT 的握手结论

POC 用的是 No Authentication，这条从未验证，而观察记录已把它记为
「S0 第一周必须单独验证，失败会阻塞 S0」。

产出 findings/U1-oauth.md：ChatGPT 实际走 DCR 还是 CIMD、发现端点
调用顺序、PKCE 是否强制、令牌 aud 的实际取值——最后一项直接决定
规格 D5「每 repo 一个端点、aud 绑定端点」能否落地。"
```

---

## Task 4: U3 —— Secure MCP Tunnel 的延迟

**Files:**
- Create: `spike/findings/U3-tunnel.md`

**Interfaces:**
- Consumes: 现有的 `poc/` 服务（仍在 8787 端口运行）
- Produces: `spike/findings/U3-tunnel.md`

**⚠️ 需要 OpenAI Platform 的 API key，且官方未公布价格 —— 开始前须获得 Human Owner 确认。**

**为什么要测**：规格 D13 决定 S0 改用 Secure MCP Tunnel 以消除公网暴露。但官方博客写明
它**当前用长轮询**且属初期方案。P-1 的 `HUMAN_GAP_MS` 阈值论证建立在「传输延迟可忽略」
之上——若隧道显著拖慢往返，该论证需重算。

- [ ] **Step 1: 装 tunnel-client 并建隧道**

```bash
curl -sL -o /tmp/tunnel-client.tar.gz \
  https://github.com/openai/tunnel-client/releases/latest/download/tunnel-client_darwin_arm64.tar.gz
tar -xzf /tmp/tunnel-client.tar.gz -C /tmp && sudo mv /tmp/tunnel-client /usr/local/bin/
tunnel-client --version
```

若发布物命名与上述不符，去 releases 页面取实际的 darwin/arm64 资产名。

在 OpenAI Platform → Tunnels 创建隧道，取得 `tunnel_id`。

- [ ] **Step 2: 指向仍在运行的 POC 服务**

```bash
export CONTROL_PLANE_API_KEY="sk-..."   # 不要写进任何会被提交的文件
tunnel-client init --profile grande-s0 --tunnel-id tunnel_xxx \
  --mcp-server-url "http://localhost:8787/$(grep POC_SECRET /Users/xtation/AgentWorks/GPT_Workspace/grande-gpt/poc/.env | cut -d= -f2)/mcp/demo-app"
tunnel-client doctor --profile grande-s0 --explain
tunnel-client run --profile grande-s0
```

- [ ] **Step 3: 在 ChatGPT 中新建走 Tunnel 的 app**

**Settings → Plugins → +** → Connection 选 **Tunnel** → 选中该隧道 → 保存。

- [ ] **Step 4: 对照测延迟**

在**同一台设备、相近时间**，分别用「Cloudflare 那个 app」和「Tunnel 这个 app」各发一次：

```
在 demo-app 上开一个任务，目标是「延迟对照」，然后跑一次单元测试。
```

两次跑完后，从服务端日志读出每次调用的间隔：

```bash
cd /Users/xtation/AgentWorks/GPT_Workspace/grande-gpt/poc
python3 -c "
import json, sys
evs=[json.loads(l) for l in open('observe-round-2.jsonl')]
prev=None
for e in evs:
    gap=f'{(e[\"ts\"]-prev)/1000:6.1f}s' if prev else '   —  '
    print(f\"  {e['iso'][11:19]}  {e['tool']:<20} 间隔={gap}  服务端处理={e['durationMs']}ms\")
    prev=e['ts']
"
```

**关注两个量**：`durationMs`（服务端自己处理耗时，两条链路应当相同）与**相邻调用间隔**
（含往返，差值即隧道引入的延迟）。

- [ ] **Step 5: 写 findings/U3-tunnel.md 并提交**

必须包含：两条链路的间隔对照表、隧道引入的延迟中位数、**轮询是否仍能在单个回合内完成**
（这是关键——若隧道把往返拖到让 20 秒 job 的轮询跨回合，P-1 在 S0 就不再成立）、
以及是否需要据此调整 `HUMAN_GAP_MS`。

```bash
cd /Users/xtation/AgentWorks/GPT_Workspace/grande-gpt
git add spike/findings/U3-tunnel.md
git commit -m "spike(U3): Secure MCP Tunnel 的延迟实测

规格 D13 决定 S0 改用该隧道以消除公网暴露，但官方明说它当前用长轮询
且属初期方案。P-1 的 HUMAN_GAP_MS 阈值论证建立在传输延迟可忽略之上，
因此切换前必须先量。

产出 findings/U3-tunnel.md：两条链路的间隔对照、隧道引入的延迟、
以及轮询是否仍能在单个回合内完成。"
```

---

## Task 5: 汇总结论并修订规格

**Files:**
- Create: `spike/findings/SUMMARY.md`
- Modify: `docs/superpowers/specs/2026-07-25-grande-gpt-s0-design.md`

**本任务无代码。**

- [ ] **Step 1: 写 SUMMARY.md**

```markdown
# S0-0 Spike 结论汇总

| 未知 | 结论 | 对 S0 的影响 |
|---|---|---|
| U1 OAuth 握手 | PASS / FAIL | ... |
| U2 Seatbelt | PASS / FAIL | ... |
| U3 Tunnel 延迟 | 中位延迟 __ms | ... |

## S0 是否可以启动
<明确的 go / no-go，以及若 no-go 则下一步做什么>
```

- [ ] **Step 2: 按结论修订规格**

- **U1 FAIL** → 规格新增一条阻塞项，**S0 暂停**。先排查是发现端点、DCR/CIMD 还是 `aud`
  的问题；仍不通则重新评估 D5（每 repo 一个端点）能否落地。
- **U2 FAIL** → **推翻 D1**（Seatbelt-only）。规格 §6 全节重写，Runner 回退到容器方案，
  §1.4 与 §11 的已知缺口相应更新。
- **U3 延迟显著**（例如中位数 >3s）→ 重新评估 D13；若保留 Tunnel，则 §6.4 的
  `pollAfterSeconds` 与报告的 `HUMAN_GAP_MS` 都要按实测重算。
- **全部 PASS** → 在 §12.1「待实测清单」中把第 1、4、5、6 条标为已完成，S0-A 可启动。

- [ ] **Step 3: 提交**

```bash
cd /Users/xtation/AgentWorks/GPT_Workspace/grande-gpt
git add spike/findings/SUMMARY.md docs/superpowers/specs/
git commit -m "spike: S0-0 结论汇总与规格修订

<一句话写清三项结论与 S0 的 go/no-go>"
```

---

## 附：Spike 完成后的清理

Spike 代码一次性，S0-A 启动时移除：

```bash
git rm -r spike/src spike/tests spike/oauth spike/package.json spike/tsconfig.json
git commit -m "chore: 移除 spike 代码，结论已固化到 findings 与规格"
```

**`spike/findings/` 保留** —— 它是 S0-C 的放行清单来源与后续争议的裁决依据。

同时清理 U1 建的临时隧道与 DNS 记录：

```bash
cloudflared tunnel route dns --overwrite-dns oauth-spike oauth-spike.agentjoey.ai  # 如需先解绑
cloudflared tunnel delete oauth-spike
```
