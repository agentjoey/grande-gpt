# GG-BL-010 C-Web-1 Formal Evidence

Date: 2026-08-23
Formal run: `C-Web-1`
Client: fresh ChatGPT Web conversation
Model: GPT-5.6 Sol
Result: **PASS**

## Scope and frozen identity

This evidence records the two disposable development tasks required by `docs/chatgpt-connector-compatibility-runbook.md` §7.2. No App Refresh/Reconnect, Scan/Refresh Tools, Gateway restart, or tool-contract change occurred between the tasks.

The production identity was stable before, during, and after the run:

- `gatewayBuild`: `git:1b9c620267137ac0af641b323c33183d3bdb13e0`
- `toolsetEpoch`: `2`
- `toolsCount`: `25`
- `toolsDigest`: `sha256:7f9d2a32ae1f0b1982f8f462c5bfe7b994e02d88466edadd74cffd5ca1eee815`

The ChatGPT Web UI did not expose a separate App version string during this run; the observed GrandeGPT tool count was 25.

## Task A

Task: `task-ggbl010-cweb1-a-20260823`
Repo: `grande-gpt`
Formal task window: successful `grande_task_open` request at `11:14:02.614` through `grande_task_close` completion at `11:16:26.308` (host log timestamps).

Coverage:

- real capability inspect (`native/grande_repo_read`);
- paginated repo map (`cursor 25 -> 50`);
- line-range repo read;
- two edits (create, then modify);
- real failing -> passing verification;
- real async job recovery with a stable `jobId`;
- task status and cleanup.

Verification:

- RED job `job_8ee8eb3f-27ef-4d0a-b987-a0f8a6ea87e7`: `1 failed / 871 passed`; the async job reached terminal state and exactly one external `grande_run_result` call fetched the terminal result.
- GREEN job `job_a9847963-321c-40ee-8b16-c1d91222be77`: `113 test files / 872 tests passed`; exactly one external `grande_run_result` call fetched its terminal result.

Gateway telemetry for the formal Task A window contains 16 canonical `[tool]` results. Sum of recorded `outputBytes`: **50,835 B**. Largest Task A result: **18,928 B**.

Before the successful Task A creation there was one malformed `grande_task_open` attempt with an invalid optional TaskBrief (`source` supplied with the wrong shape). It did not create the disposable task and is therefore not included in the formal Task A task window or its canonical result-byte total. The rejection is retained here as a setup anomaly rather than silently rewritten as a successful call; no `tool disabled`, `Resource not found`, 401, Gateway restart, or identity drift accompanied it.

## Task B

Task: `task-ggbl010-cweb1-b-20260823`
Repo: `grande-obsidian-mcp`
Formal task window: `grande_task_open` request at `11:16:31.123` through final required `grande_repo_read` at `11:17:57.459`.

Task B began about 4.8 seconds after Task A close completed, well inside the required five-minute limit.

Coverage:

- line-range repo read;
- paginated repo map (`cursor 15 -> 30`);
- two edits (create, then modify);
- real failing -> passing verification;
- final `grande_task_status` and `grande_repo_read` probes after cleanup.

Verification:

- RED job `job_867db2a0-e047-49c4-b79e-9f584e4e88b4`: `1 failed / 39 passed`.
- GREEN job `job_9bb7bf82-bda7-40e1-a624-c56b614a0010`: `12 test files / 40 tests passed`.

Gateway telemetry for the Task B window contains 14 canonical `[tool]` results. Sum of recorded `outputBytes`: **37,837 B**. Largest Task B result: **8,176 B**.

## Budget and boundary reconciliation

Measured canonical results for Task A + Task B:

- external canonical calls: `16 + 14 = 30`;
- Task A output bytes: `50,835 B`;
- Task B output bytes: `37,837 B`;
- combined serialized MCP result bytes: **88,672 B** (`< 1 MiB`);
- maximum single serialized result: **18,928 B** (`< 32 KiB`).

Each measured current-run call in the host log has the expected `/mcp -> [rpc] tools/call -> [tool]` sequence. Gateway telemetry reported `correlation=none` for these requests because no `Mcp-Session-Id` correlation value was present; the evidence records that server-generated value as-is and does not invent a conversation identifier.

During the `11:14-11:18` formal task window:

- `The GrandeGPT tool has been disabled`: none observed;
- `Resource not found`: none observed;
- unexpected 401 / invalid bearer: none observed;
- Gateway restart/startup marker: none observed;
- stderr in the queried window: empty;
- `gatewayBuild / toolsetEpoch / toolsCount / toolsDigest`: stable.

## Gate decision

`C-Web-1` satisfies the §7.2 two-task formal-run criteria and is recorded as **PASS**.

This is only the first row of the three-run formal matrix. `GG-BL-010` remains **MITIGATED** until `C-iOS` and `C-Web-2` also pass and the subsequent seven-day ordinary-use observation completes.