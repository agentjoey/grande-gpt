# GG-BL-010 C-Web-2 Formal Evidence

Date: 2026-08-23
Formal run: `C-Web-2`
Client: second fresh ChatGPT Web conversation
Model: GPT-5.6 Sol
Result: **PASS**

## Scope and frozen identity

This is the third and final independent run in `docs/chatgpt-connector-compatibility-runbook.md` §7.2. It does not reuse the C-Web-1 or C-iOS disposable tasks, call budgets, or runtime evidence.

The production identity was stable before, during, and after the run:

- `gatewayBuild = git:1b9c620267137ac0af641b323c33183d3bdb13e0`
- `toolsetEpoch = 2`
- `toolsCount = 25`
- `toolsDigest = sha256:7f9d2a32ae1f0b1982f8f462c5bfe7b994e02d88466edadd74cffd5ca1eee815`

No Refresh/Reconnect, Scan/Refresh Tools, Gateway restart, contract change, or artificial long idle occurred between Task A and Task B.

## Task A

Disposable task: `task-ggbl010-cweb2-a-20260823`

Coverage included real repository inspection, native capability inspection, line-range/bounded reads, two real edits, intentional failing verification followed by passing verification, async job recovery, task status, diff inspection, and cleanup.

Verification jobs:

- RED: `job_19b0b96f-4c7d-4a17-bd1f-9bd07f18de00`, terminal `failed`, exit code 1; exactly one external `grande_run_result` call fetched its terminal result.
- GREEN: `job_2edd258d-63df-4474-af9f-859c1918e2a2`, terminal `passed`, confirmed through `grande_task_status`; no unnecessary external `grande_run_result` call was made.

One initial `grande_task_open` request returned the expected ordinary `INVALID_INPUT` application error because the optional TaskBrief supplied an invalid `source` shape. It reached Gateway, is included in the Task A call/byte budget, created no task, and was not an auth or binding failure.

Task A budget:

- external GrandeGPT calls: **16 / 50**
- canonical delivered MCP output bytes: **46,776 bytes**

The disposable worktree and branch were removed. No push, PR, or merge was performed for the probe task.

## Task B

Disposable task: `task-ggbl010-cweb2-b-20260823`

Task B was independently created after Task A cleanup. It covered bounded/line-range repo inspection, search, two real edits, intentional failing verification followed by passing verification, async job recovery, diff/status inspection, and cleanup.

Verification jobs:

- RED: `job_45f8a874-abf4-46f9-8cbd-b92fd5f0b897`, terminal `failed`, exit code 1; exactly one external `grande_run_result` call fetched its terminal result.
- GREEN: `job_d4e90e25-75c9-4d51-806d-525b233585cd`, terminal `passed`, confirmed through `grande_task_status`; no unnecessary external `grande_run_result` call was made.

Task B budget:

- external GrandeGPT calls: **14 / 50**
- canonical delivered MCP output bytes: **22,897 bytes**

Task A close → Task B open gap: **10.747 seconds**, safely below the five-minute limit.

The disposable worktree and branch were removed. No push, PR, or merge was performed for the probe task.

## MCP result budget reconciliation

Gateway telemetry records `outputBytes` using the runbook's canonical delivered MCP-result definition: the UTF-8 byte size of `JSON.stringify(toMcpTextResult(envelope))`.

Measured results:

- Task A: **46,776 bytes**
- Task B: **22,897 bytes**
- Task A + Task B: **69,673 bytes** `< 1,048,576 bytes (1 MiB)`
- entire 35-call C-Web-2 window, including three preflight calls and two final probes: **99,830 bytes**
- maximum single result in the entire window: **18,928 bytes** `< 32,768 bytes (32 KiB)`
- `outputBytes=unknown`: **0**

## Gateway boundary reconciliation

The first reconciliation script reported the final call as `MISSING`, but this was a collection-script directionality error rather than a Gateway failure. The script searched for `POST /mcp` after each `[tool]` line, while the observed Gateway logging order is:

`[rpc] tools/call → [gw] POST /mcp → 200 → [tool]`

Human Owner supplied the exact final Host log slice:

- `[rpc] 12:25:37.827 tools/call #0`
- `[gw] 12:25:37.828 POST /mcp → 200 (15ms)`
- `[tool] 12:25:37.830 grande_repo_read correlation=none inputBytes=118 outputBytes=3650 ... result=ok durationMs=1`

This closes the only outstanding C-Web-2 boundary item. The final required `grande_repo_read` therefore reached Gateway, received HTTP 200 on `/mcp`, completed successfully, and produced a canonical numeric `outputBytes` result.

The Host window also reported:

- `POST /mcp 401 = 0`
- no invalid bearer/auth failure attributable to the formal call path
- no SIGTERM / Gateway restart
- no toolset identity change
- `rpc tools/call`, `[tool]`, and `POST /mcp` counts balanced in the inspected window
- Gateway-generated `correlation=none` was preserved as observed; no synthetic correlation was invented

## Failure monitoring and final probes

Observed during C-Web-2:

- `The GrandeGPT tool has been disabled`: **0**
- `Resource not found`: **0**
- unexpected 401 / auth failure: **0**
- Gateway restart: **0**
- identity drift: **0**
- App/session binding failure: **0**

After Task B cleanup, in the same conversation:

- final `grande_task_status`: **PASS**
- final `grande_repo_read`: **PASS**, with the final Host boundary now explicitly reconciled

## Formal gate decision

C-Web-2 satisfies the §7.2 hard pass criteria:

- two independent disposable development tasks completed and were cleaned up
- Task A `16 ≤ 50` calls
- Task B `14 ≤ 50` calls
- Task A + B `69,673 bytes ≤ 1 MiB`
- maximum single result `18,928 bytes ≤ 32 KiB`
- real failing → passing verification occurred in both tasks
- stable async job IDs were exercised; each externally fetched terminal job received exactly one `grande_run_result`
- Task B began 10.747 seconds after Task A cleanup
- final `grande_task_status` and `grande_repo_read` succeeded
- no disabled / Resource not found / unexpected 401 / restart / identity drift occurred
- frozen production identity remained stable
- the formerly missing final boundary item is now closed by exact Host telemetry

**C-Web-2 formal validation: PASS.**

Therefore the §7.2 formal matrix is now:

**GG-BL-010 formal matrix = 3/3 PASS (C-Web-1 + C-iOS + C-Web-2).**

This does **not** close `GG-BL-010`. Its status remains `MITIGATED`, and the next required stage is the runbook §7.3 seven-day ordinary-use observation: at least five ordinary conversations, at least two real user tasks per conversation, Web plus the currently capability-supported iOS target, with no unexplained disablement before `DONE` is allowed.
